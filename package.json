{
  "name": "bmapp2",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^8.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.31",
    "@types/dotenv": "^6.1.1",
    "@types/node": "^10.14.7",
    "netlify-lambda": "^1.5.1",
    "rimraf": "^2.6.3",
    "typescript": "^3.5.3"
  },
  "scripts": {
    "build:clean": "rimraf app && rimraf common && rimraf functions",
    "build": "npm run build:clean && tsc && netlify-lambda build functions-src",
    "serve": "netlify-lambda -s serve functions"
  }
}
